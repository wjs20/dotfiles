#!/usr/bin/env bash

set -euxo pipefail
curl -LO https://github.com/neovim/neovim/releases/latest/download/nvim-linux-x86_64.tar.gz
rm -rf .local/opt/nvim
tar -C .local/opt -xzf nvim-linux-x86_64.tar.gz

